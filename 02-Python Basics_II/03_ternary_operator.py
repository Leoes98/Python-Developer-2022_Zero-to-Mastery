# Ternary Operator
# It's a operation that evaluates on something based on the condition being True or False

#condition_iftrue if condition else condition_if_else
is_friend = True
can_message = 'message allowed' if is_friend else 'not allowed to message'

print(can_message)
